<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Top menu item -->
    <menuitem
        id="real_estate_root_menu"
        name="Real Estate"
        sequence="10"/>

    <!-- Submenu item -->
    <menuitem
        id="real_estate_property_menu"
        name="Property"
        parent="real_estate_root_menu"
        action="action_real_estate_root"
        sequence="10"/>

    <!-- Start add at 20250111-1447: Add Settings child menu -->
    <menuitem
        id="menu_real_estate_settings"
        name="Settings"
        parent="real_estate_root_menu"
        sequence="20"/>
    <!-- End add at 20250111-1447: Add Settings child menu -->

    <!-- Start add at 20241220-1455: Add Property Types menu with action -->
    <record id="action_property_type" model="ir.actions.act_window">
        <field name="name">Property Types</field>
        <field name="res_model">y.realestate.property.type</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem
        id="menu_property_type"
        name="Property Types"
        parent="menu_real_estate_settings"
        action="action_property_type"
        sequence="20"/>
    <!-- End add at 20241220-1455: Add Property Types menu with action -->
</odoo>